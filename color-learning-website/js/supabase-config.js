/**
 * Fill these values from your Supabase project settings before public deployment.
 * Keep the anon/publishable key here only for client-side browser access.
 */
(function () {
  "use strict";

  window.CLWSupabaseConfig = window.CLWSupabaseConfig || {
    url: "",
    anonKey: "",
    schema: "public"
  };
})();
